meta {
  name: Detailed state (JSON)
  type: http
  seq: 7
}

post {
  url: {{BASE_URL}}/detailedstate
  body: json
  auth: inherit
}

headers {
  Accept: application/json
}

body:json {
  {
    "issuer": {
      "id": "DrMint",
      "secret": "{{p1Secret}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
