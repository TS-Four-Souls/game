meta {
  name: Detailed state
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/detailedstate
  body: json
  auth: inherit
}

body:json {
  {
    "issuer": {
      "id": "DrMint",
      "secret": "{{p1Secret}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
