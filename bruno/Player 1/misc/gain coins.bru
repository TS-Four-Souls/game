meta {
  name: Gain 5 coin
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/gaincoins
  body: json
  auth: inherit
}

body:json {
  {
    "issuer": {
      "id": "DrMint",
      "secret": "{{p1Secret}}"
    },
    "coins": 5
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
