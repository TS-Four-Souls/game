meta {
  name: Finish turn
  type: http
  seq: 3
}

post {
  url: {{BASE_URL}}/next
  body: json
  auth: inherit
}

body:json {
  {
    "issuer": {
      "id": "DrMint",
      "secret": "{{p1Secret}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
