meta {
  name: Get in play
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/inplay
  body: json
  auth: inherit
}

body:json {
  {
    "issuer": {
      "id": "DrMint",
      "secret": "{{p1Secret}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
