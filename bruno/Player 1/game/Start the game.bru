meta {
  name: Start the game
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/start
  body: json
  auth: inherit
}

body:json {
  {
    "issuer": {
      "id": "DrMint",
      "secret": "{{p1Secret}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
