meta {
  name: Reset the game
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/reset
  body: json
  auth: inherit
}

body:json {
  {
    "issuer": {
      "id": "DrMint",
      "secret": "{{p1Secret}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
