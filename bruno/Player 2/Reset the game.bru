meta {
  name: Reset the game
  type: http
  seq: 5
}

post {
  url: {{BASE_URL}}/reset
  body: json
  auth: inherit
}

body:json {
  {
    "issuer": {
      "id": "slichau",
      "secret": "{{p2Secret}}"
    }
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
