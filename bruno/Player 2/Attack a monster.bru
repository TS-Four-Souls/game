meta {
  name: Attack a monster
  type: http
  seq: 2
}

post {
  url: {{BASE_URL}}/attack
  body: json
  auth: inherit
}

body:json {
  {
    "issuer": {
      "id": "slichau",
      "secret": "{{p2Secret}}"
    },
    "monsterId": "Monster 1"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
